package yashna;

public class Test {
	/*public static void main(String[] args) {
		Test testObj = new Test();
		int s1[]={11,29,80};
		int s2[]={14,19,27};
		testObj.merge(s1,s2);
	}
*/
	
	static{
		  System.out.print("nirvan ");
		  System.exit(0);
		 }
	/*public void merge(int a[],int b[]) {
		int x = 0;
		int y = 0;
		int nums[] = new int[a.length + b.length];
		int i;
		for (i = 0; x < a.length && y < b.length; i++) {
			if (a[x] < b[y]) {
				nums[i] = a[x];
				x++;
			} else {
				nums[i] = b[y];
				y++;
			}
		}

		while (x < a.length) {
			nums[i] = a[x];
			i++;
			x++;
		}

		while (y < b.length) {
			nums[i] = b[y];
			i++;
			y++;
		}	
		
		for (int k = 0; k < nums.length; k++) {
			System.out.print(nums[k] + " ");
		}

	}*/
}
